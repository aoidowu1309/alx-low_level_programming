#include<stdio.h>
/*
 * main - Start the coding here
 * Description: print the alphabet in lower case
 * Return: Always 0 (success)
 */
int main(void)
	int ch;
	clrscr();
	for (ch= 'a'; ch<= 'Z'; ch++)
	putchar(ch);
	putchar('n\');
	getch();
{
